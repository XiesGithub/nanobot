# 独立阅读与实现能力改进计划

**目标：** 将现有 9 章源码导览补成可脱离仓库阅读、有真实代码、有数据流和实现验收的学习手册。

**范围：** 只改 `study/`。原有章节和 211 道折叠答案保留；新内容静态嵌入，不依赖运行中的 nanobot、CDN 或模型调用。所有项目命令使用 `conda run -n nanobot`。

## 验收驱动的任务

- [x] 审计原有代码量、图示类型、仓库外跳链接和学习目标，写入 `study/design/2026-09-05-handbook-audit.md`。
- [x] 在 01—09 章各新增 `workshop.html`，包含数据契约、带标签和失败分支的 SVG、至少三段经当前源码核对的连续摘录、执行轨迹、设计练习与参考验收。
- [x] 新增第 10 章和 `study/examples/`，提供标准库即可运行的模块化教学 Agent、完整内嵌代码与行为测试。
- [x] 更新 `assets/catalog.js`、首页和各章入口。原源码地图链接改为手册内部的相应讲解或摘录，保留来源元数据，不伪装成精确原行定位。
- [x] 增强 `tools/check_study.py`：检查本地链接不逃出 study、fragment 有效、目录覆盖、摘录元数据、图示和离线资源；可选 `--check-source` 校验真实摘录。
- [x] 用负向样例验证外跳、坏锚点、源码漂移、SVG引用与边界；通过原有问答、真实导航脚本与实作行为检查。
- [x] 独立复核技术内容和实现边界，修正超时、忙闲子代理和图示分支问题。

浏览器视觉检查受工具 URL 安全策略阻止，未绕过、未声称完成。替代验证仅覆盖静态 SVG 结构、节点边界、资源和导航行为，不等同于实际浏览器渲染。详见审计报告的验收记录。

## 责任边界

模块内容按 01—04、05—09 分为两个互不写同一文件的任务；综合实作单独负责第 10 章及 examples；主任务负责审计、链接、样式、检查与整合。

## 阅读设计

保留原来用于建立全景的专题页；每章增加一次“读真实代码 → 演算数据 → 自己设计 → 验收”的闭环。首页提供新读者的实现顺序，先完成最小闭环再扩展长期记忆与后台任务。源码文件路径只是来源标记，学习内容和运行教学示例均不要求仓库存在。
